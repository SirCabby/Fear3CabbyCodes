#pragma once

// F3CC_VERSION is defined by the build from the VERSION file (see the Makefile).
// The fallback only applies to ad-hoc compiles that bypass it.
#ifndef F3CC_VERSION
#define F3CC_VERSION "0.0.0-dev"
#endif
